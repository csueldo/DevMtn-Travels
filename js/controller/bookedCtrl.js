angular.module('devmtnTravel')
  .controller('bookedCtrl', function($scope, mainSrv){
    // write a function that checks the url params and then loops over the data object in the service and then returns the object that matches the id being passed in the url params. Do this in your controller




  });
